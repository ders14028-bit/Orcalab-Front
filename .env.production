# Usado automáticamente por `vite build`: el bundle de producción lleva esta
# URL horneada (el sitio estático en S3 no tiene proxy de Vite).
# Si el ALB se recrea con terraform, actualizar y volver a desplegar el front.
VITE_API_BASE_URL=http://orcalab-alb-1090154343.us-east-1.elb.amazonaws.com
